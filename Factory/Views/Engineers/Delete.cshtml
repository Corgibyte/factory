@model Factory.Models.Engineer

<h2>Are you sure you want to delete this engineer?</h2>
<h6>This cannot be undone!</h6>

<div class="inner-pane">
  <h3>@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Name)</h3>

  @using (Html.BeginForm())
  {
    <button class="btn btn-input" type="submit">Delete engineer</button>
  }
</div>

@Html.ActionLink("Back to engineer", "Details", new { id = Model.EngineerId }, new {@role="button", @class="btn"})